{% extends "j0k3rFeedBundle::layout.html.twig" %}

{% block h2_title %}Testing item{% endblock %}

{% block content %}
<div class="row">
    <div class="large-12 columns">
        <form action="{{ path('feed_test') }}" method="post" {{ form_enctype(form) }} class="custom">
            {{ form_errors(form) }}

            <div class="row">
                <div class="large-7 columns">
                    <div class="row collapse">
                        <div class="small-10 columns">
                            {{ form_widget(form.link) }}
                        </div>
                        <div class="small-2 columns">
                            <span class="postfix radius">{{ form_label(form.link) }}</span>
                        </div>
                    </div>
                </div>
                <div class="large-3 columns">
                    <div class="row collapse">
                        <div class="small-9 columns">
                            {{ form_widget(form.parser) }}
                        </div>
                        <div class="small-3 columns">
                            <span class="postfix radius">{{ form_label(form.parser) }}</span>
                        </div>
                    </div>
                </div>
                <div class="large-2 columns">
                    <div class="row collapse">
                        <button type="submit" class="small button radius success">Test</button>
                    </div>
                </div>
            </div>
            {{ form_rest(form) }}
        </form>
    </div>

    {% if content.url is defined %}
    <hr/>
    <div class="large-12 columns">
        {{ include('j0k3rFeedBundle:FeedItem:content.html.twig', {'title': '', 'content': content.content, 'url': content.url, 'modal': false}) }}
    </div>
    {% endif %}
</div>

{% endblock %}
